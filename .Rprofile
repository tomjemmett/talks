source("renv/activate.R")

# {icons}
# set the icon.path option to save to a specific folder which we can cache
# would like to use here, but this script may get loaded before renv::restore()
options("icon.path" = file.path(getwd(), "icons"))
